The DEV frontend is running at localhost:5173
The DEV backend is running at localhost:3000

Calls to the backend are proxied to :3000 via vite proxy for dev

Dont use overly exaggerated comments - keep it clean
Keep the code clean in general. Built it as maintainable and dynamic as possible.
Dont use hardcoded/hacky stuff.

For the frontend, use Vue 3. Use the Composition API for Vue 3.

For the backend, you are using Nest.JS.

MongoDB and Postgres are used together with Mongoose and TypeORM for databases. Redis is used for caching.